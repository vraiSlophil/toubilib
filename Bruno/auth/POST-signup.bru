meta {
  name: POST-signup
  type: http
  seq: 2
}

post {
  url: {{API_SRC}}/auth/signup
  body: json
  auth: inherit
}

body:json {
  {
    "email": "{{patientEmail}}",
    "password": "P@ssw0rd"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
