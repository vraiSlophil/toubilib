meta {
  name: DELETE-rdv
  type: http
  seq: 4
}

delete {
  url: {{API_SRC}}/rdvs/{{rdvId}}
  body: none
  auth: inherit
}

headers {
  ~Authorization: Bearer {{accessToken}}
}

settings {
  encodeUrl: true
  timeout: 0
}
