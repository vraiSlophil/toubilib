meta {
  name: GET-rdv
  type: http
  seq: 2
}

get {
  url: {{API_SRC}}/rdvs/{{rdvId}}
  body: none
  auth: inherit
}

headers {
  ~Authorization: Bearer {{accessToken}}
}

settings {
  encodeUrl: true
  timeout: 0
}
